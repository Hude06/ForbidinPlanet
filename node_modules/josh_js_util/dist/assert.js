export function equals_any(a, b) {
    if (Array.isArray(a) && Array.isArray(b)) {
        return equals_array(a, b);
    }
    return (a === b);
}
export function equals_array(a1, b1) {
    if (a1.length !== b1.length)
        return false;
    for (let i = 0; i < a1.length; i++) {
        let a = a1[i];
        let b = b1[i];
        if (!equals_any(a, b))
            return false;
    }
    return true;
}
export function assert_eq(message, a, b) {
    let matched = equals_any(a, b);
    if (!matched) {
        console.error(`${message} failed`);
        console.error(a);
        console.error('not equal to');
        console.error(b);
        throw new Error(`${message}`);
    }
    console.info("PASSED:", message);
}
//# sourceMappingURL=assert.js.map