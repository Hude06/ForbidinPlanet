import { Point } from "./index.js";
export declare class ArrayGrid<C> {
    w: number;
    h: number;
    data: C[];
    constructor(w: number, h: number);
    set_at(x: number, y: number, value: C): void;
    get_at(x: number, y: number): C;
    forEach(cb: (value: C, index: Point) => void): void;
    fill(cb: (index: Point) => C): void;
    size(): number;
    set_from_list(list: C[]): void;
    static fromPattern<C>(pattern: string, cb: (ch: string, index: Point) => C): ArrayGrid<C>;
}
