export class JArray extends Array {
    static of(...args) {
        return new JArray(...args);
    }
    static range(first) {
        let arr = new JArray();
        for (let i = 0; i < first; i++)
            arr.push(i);
        return arr;
    }
    constructor(...args) {
        super(...args);
    }
    map(fn) {
        return super.map(fn);
    }
    tap(fn) {
        this.forEach(fn);
        return this;
    }
    filter(fn) {
        return super.filter(fn);
    }
    pluck(name) {
        return this.map((x => x[name]));
    }
    first() {
        return this[0];
    }
    last() {
        return this[this.length - 1];
    }
    empty() {
        return this.length === 0;
    }
}
//# sourceMappingURL=jarray.js.map